# คู่มือ: แยกสายอีเมล WH / TRO (ผู้จัดการไซต์ / ผู้จัดการจัดส่ง)

ระบบแยกผู้ถูกตรวจเป็น 2 ประเภทตั้งแต่หน้าฟอร์มแล้ว และต่อรหัสสังกัดให้อัตโนมัติ:

| ประเภทในฟอร์ม | ค่า `personType` | สังกัดต่อท้าย | อีเมลถึง |
|---|---|---|---|
| พนักงานทั่วไป | `general` | `<บริษัท> / WH` | **ผู้จัดการไซต์** |
| พนักงานขนส่ง (มีทะเบียนรถ) | `transport` | `<บริษัท> / TRO` | **ผู้จัดการจัดส่ง** |

- ฟอร์ม (`index.html`) เติม `/ WH` หรือ `/ TRO` ต่อท้ายชื่อบริษัทตอนส่ง → SharePoint/อีเมลเห็นสังกัดชัดเจน
- เทมเพลตอีเมล (`email-template.html`) เปลี่ยนคำขึ้นต้นเป็น **เรียน ผู้จัดการจัดส่ง** / **เรียน ผู้จัดการไซต์** อัตโนมัติตาม `personType` แล้ว
- เหลือสิ่งเดียวที่ต้องตั้งใน **Power Automate**: ส่งอีเมลไปยังผู้รับให้ถูกสาย

---

## วิธีที่ 1 (แนะนำ) — ทำช่อง To เป็นเงื่อนไข (แก้ Send Email เดิม 1 จุด)

เพราะแต่ละรายงานคือคน 1 คน (เป็น WH หรือ TRO อย่างใดอย่างหนึ่ง) จึงส่งอีเมล 1 ฉบับต่อรายงาน
ไม่ต้องทำ 2 action แค่ทำให้ปลายทางเปลี่ยนตามประเภท

1. เปิด Flow → action **Send an email (V2)**
2. ที่ช่อง **To** ลบค่าเดิมออก แล้วใส่นิพจน์ (Expression) นี้:

   ```
   if(equals(body('Parse_JSON')?['personType'],'transport'),'delivery-manager@company.com','site-manager@company.com')
   ```

3. แทนที่ 2 อีเมลด้วยของจริง:
   - `delivery-manager@company.com` → อีเมล **ผู้จัดการจัดส่ง** (สาย TRO)
   - `site-manager@company.com` → อีเมล **ผู้จัดการไซต์** (สาย WH)
4. คำขึ้นต้นในเนื้ออีเมลจัดการโดยเทมเพลตแล้ว — ไม่ต้องแก้ Body
5. **Save**

> ต้องการหลายผู้รับต่อสาย? คั่นด้วย `;` เช่น
> `if(equals(body('Parse_JSON')?['personType'],'transport'),'a@co.com;b@co.com','c@co.com')`

---

## วิธีที่ 2 (ทางเลือก) — แยก 2 Condition branch ชัดเจน

ถ้าต้องการให้ 2 สายมี Send Email แยก action (เช่นเนื้อหา/CC ต่างกันมากในอนาคต):

1. เพิ่ม **Condition** หลัง `Parse_JSON`
   - เงื่อนไข: `body('Parse_JSON')?['personType']` **is equal to** `transport`
2. **If yes (TRO)** → Send an email → To = อีเมลผู้จัดการจัดส่ง
3. **If no (WH)** → Send an email → To = อีเมลผู้จัดการไซต์
4. เนื้ออีเมลใช้เทมเพลตเดียวกัน (`email-template.html`) ได้ทั้งสอง branch เพราะคำขึ้นต้นเป็นเงื่อนไขในตัวอยู่แล้ว

---

## Prompt สำหรับ Claude ใน Chrome (ถ้าให้ตั้งให้)

> เปิด Power Automate flow ของ "alcohol positive" ไปที่ action **Send an email (V2)**
> ที่ช่อง **To** ให้ลบค่าเดิม แล้วใส่เป็น Expression นี้:
> `if(equals(body('Parse_JSON')?['personType'],'transport'),'ใส่อีเมลผู้จัดการจัดส่ง','ใส่อีเมลผู้จัดการไซต์')`
> แทนข้อความ 2 จุดด้วยอีเมลจริงตามที่ฉันบอก แล้วกด Save
> ยืนยันว่า Save สำเร็จ 0 error

---

## ทดสอบ

1. ส่งฟอร์ม 1 ครั้งแบบ **พนักงานทั่วไป** → SharePoint สังกัด = `<บริษัท> / WH`, อีเมลขึ้นต้น "เรียน ผู้จัดการไซต์" และถึงผู้จัดการไซต์
2. ส่งฟอร์ม 1 ครั้งแบบ **พนักงานขนส่ง** → สังกัด = `<บริษัท> / TRO`, อีเมลขึ้นต้น "เรียน ผู้จัดการจัดส่ง" และถึงผู้จัดการจัดส่ง
3. ตรวจ Run history ว่าทั้งสอง Succeeded และปลายทางถูกสาย
